# บันทึกการทำงาน — 7 กันยายน 2026

Workshop ท้าย Lecture 10 วิชา 310-3311 Image Processing
หัวข้อ: การแบ่งส่วนดอกทานตะวันออกจากพื้นหลัง

---

## สรุปสั้น

| | |
|---|---|
| เขียนเสร็จ | `step1` โหลดภาพ · `step2` เครื่องมือระบายเฉลย · `step3` ตัดค่าสี · `step4` morphology |
| เอกสาร | `README.md` · `requirements.txt` · `.gitignore` |
| ยังไม่ได้เขียน | `step5` Confusion Matrix · `step6` ROC Curve · `index.html` |
| ติดอยู่ที่ | ground truth ยัง 0 จาก 50 ภาพ ต้องระบายด้วยมือ |
| จำนวน def | 15 ตัว (เคยมี 18 ตัดออก 3) |
| ปัญหาที่เจอและแก้แล้ว | 15 เรื่อง |

---

## สิ่งที่ตัดสินใจ และเหตุผล

### 1. เลือกดอกทานตะวันเป็นวัตถุ

ใช้ dataset **PhotoArt50** จาก University of Bath คลาส `204.sunflower` เอาเฉพาะรูปถ่ายจริง 50 ภาพ

ตรวจความปลอดภัยของ dataset ก่อนใช้ — ไล่ทุกไฟล์ในรีโปต้นทาง 5,427 ไฟล์ มีแค่ 4 นามสกุล
`.jpg` 5,375 ไฟล์ · `.txt` 50 · `.md` 1 · `.bib` 1
ไม่มีไฟล์รันได้ปนมาเลย ไม่มีสคริปต์ที่ต้องรันก่อนใช้ข้อมูล

เหตุผลที่เลือกทานตะวัน: **เกสรกลางดอกสีเข้ม** ทำให้ threshold สีเหลืองได้ผลเป็นวงโดนัทมีรูตรงกลาง
ซึ่งเป็นโจทย์ที่ Morphology แก้ได้ตรงๆ ทำให้โจทย์ข้อ 4 มีผลต่างให้เห็นเป็นตัวเลขจริง

### 2. ต้องสร้าง ground truth เอง

PhotoArt50 มี ground truth ให้ แต่เป็น **กรอบสี่เหลี่ยม** ไม่ใช่ mask ระดับพิกเซล

วัดกับภาพ `204p_0001.jpg`
```
พื้นที่ในกรอบ                       36,764 px
ดอกทานตะวันจริงที่อยู่ในกรอบ         21,219 px
พื้นหลังที่กรอบบอกผิดว่าเป็นดอกไม้    15,545 px  =  42.3% ของกรอบ
```

ถ้าใช้กรอบเป็นเฉลย algorithm ที่แยกดอกไม้ได้แม่นที่สุดจะได้คะแนนแย่ที่สุด
โจทย์ข้อ 2 อนุญาตไว้แล้วว่า "ถ้าไม่มี ให้สร้างขึ้นมาเอง"

### 3. ห้ามให้โปรแกรมสร้าง ground truth

ทดลองดูว่าถ้าเอาผลของ `step4` ไปตั้งเป็นเฉลยเสียเลย จะวัดผลได้เท่าไหร่

```
TP = 1,847,095    FP = 0
FN = 0            TN = 2,017,119
Accuracy = 100.0%   Precision = 100.0%   Recall = 100.0%
```

ไม่มีความผิดพลาดเลยสักพิกเซลใน 50 ภาพ ซึ่งเป็นไปไม่ได้ และ ROC จะเป็นมุมฉากชิดขอบ ไม่มีอะไรให้อภิปราย
ground truth จึงต้องมาจากมือคนเท่านั้น

### 4. ใช้ Lab ช่อง b ไม่ใช่ HSV

โจทย์ข้อ 5 ต้องการ ROC Curve ซึ่งสไลด์หน้า 59 นิยามว่าเป็นผลของ classifier
**at varying threshold values** คือต้องกวาดค่าตัด "ค่าเดียว"

`cv2.inRange` บน HSV ต้องกำหนดตัวเลข 6 ตัว (ขอบล่าง-ขอบบนของ H S V) กวาดไม่ได้
Lab ช่อง b วัดความเป็น น้ำเงิน↔เหลือง ตรงๆ ได้ตัวเลขเดียวต่อพิกเซล

วัดกับภาพจริง 5 ภาพที่พื้นหลังคนละแบบ

| ภาพ | พื้นหลัง | b ในดอก | b พื้นหลัง | ห่างกัน |
|---|---|---|---|---|
| 0001 | ดำ | 193 | 131 | 61 |
| 0020 | ฟ้า | 178 | 114 | 64 |
| 0009 | ใบไม้เขียว | 183 | 142 | 40 |
| 0033 | เทา | 189 | 146 | 43 |
| 0043 | เทาซีด | 181 | 133 | 48 |

ลองช่อง Hue เทียบแล้วแย่กว่ามาก เพราะ Hue คือมุมของสี พิกเซลสีเทา ดำ ขาว ไม่มีสี
จึงไม่มีมุมสีที่มีความหมาย ค่าที่ได้กระโดดมั่ว ในขณะที่พื้นหลังของ dataset นี้มีทั้งดำ เทา และขาว

### 5. ลำดับ Morphology ต้องเป็น Opening → Closing → Region Filling

| ขั้น | แก้ปัญหาอะไร | สไลด์ |
|---|---|---|
| Opening | จุดขาวเล็กๆ ในพื้นหลัง จากใบไม้หรือดอกอื่นที่บังเอิญเหลืองพอ | หน้า 24 |
| Closing | วงกลีบดอกขาดเป็นตัว C เพราะกลีบบางกลีบสีเข้มไป | หน้า 24 |
| Region Filling | เกสรกลางดอกเป็นรูดำ ทำให้ mask เป็นวงโดนัท | หน้า 34-35 |

### 6. ไม่ทำ client/server แบบ V1

โจทย์หน้า 63 ไม่ได้ขอ (client/server เป็นโจทย์ของ Lecture 9 คนละใบ)
ใช้สคริปต์ local ล้วน แล้วมี `index.html` แสดงรูปกับกราฟอย่างเดียว ไม่มี server ไม่มี JavaScript

---

## ปัญหาที่เจอ และวิธีแก้

เรียงตามลำดับที่เจอจริง

### 1. UnicodeEncodeError ตอน print ภาษาไทย

**อาการ** `UnicodeEncodeError: 'charmap' codec can't encode characters`

**สาเหตุ** วัดได้ว่า
```
รันใน terminal ตรงๆ        sys.stdout.encoding = utf-8    print ไทยได้
รันแล้วส่ง output ไปที่อื่น   sys.stdout.encoding = cp1252   print ไทยแล้วพัง
```
เวลา output ไม่ได้ออกหน้าจอตรงๆ Python จะหันไปใช้ค่า locale ของเครื่องแทน
ซึ่งเป็น cp1252 (ตารางตัวอักษรยุโรปตะวันตก) ที่ไม่มีตัวอักษรไทยอยู่เลย

**วิธีแก้** `sys.stdout.reconfigure(encoding="utf-8")`

### 2. FileNotFoundError หาโฟลเดอร์ dataset ไม่เจอ

**อาการ** `FileNotFoundError: The system cannot find the path specified: 'dataset/images'`
ทั้งที่ `step1` รันผ่านไปแล้ว

**สาเหตุ** path แบบอ้างอิงอ้างอิงจาก **โฟลเดอร์ที่เทอร์มินัลยืนอยู่** ไม่ใช่โฟลเดอร์ที่ไฟล์ `.py` วางอยู่
พอกดปุ่ม Run ในโปรแกรมแก้โค้ด มันยืนอยู่ที่โฟลเดอร์ที่เปิดไว้ ซึ่งไม่ใช่โฟลเดอร์โปรเจกต์
ผลคือ `step1` ไปสร้างภาพ 50 ไฟล์ไว้นอกโฟลเดอร์โปรเจกต์ **โดยไม่ error เลย**
เพราะ `os.makedirs` สร้างให้ไม่ว่าจะอยู่ตรงไหน
พบว่ามีโฟลเดอร์ `dataset/images` อยู่ 2 ที่ ที่ละ 50 ไฟล์

**วิธีแก้** ให้โปรแกรมหาโฟลเดอร์ตัวเองก่อน
```python
PROJECT_FOLDER = os.path.dirname(os.path.abspath(__file__))
IMAGE_FOLDER = os.path.join(PROJECT_FOLDER, "dataset", "images")
```
`os.path.join` ใส่ตัวคั่นให้ถูกตามระบบด้วย Windows ได้ `\` Mac ได้ `/` ไม่ต้องแก้โค้ด

### 3. Closing อุดรูเกสรไม่ได้

**อาการ** ตั้งใจใช้ Closing อุดรูตรงกลางดอก แต่ไม่ได้ผล

**สาเหตุ** สไลด์หน้า 24 เขียนครบว่า *"fills holes **smaller than the structuring element**"*
เกสรกลางดอกใหญ่กว่า SE มาก

**หลักฐาน** วัดกับ `204p_0001.jpg` ด้วย SE วงรี 15×15
```
20,052 -> 21,318 px   เพิ่มแค่ 6%   รูยังโหว่อยู่
```

**วิธีแก้** ใช้ Region Filling แทน ซึ่งครูเขียนกำกับไว้ในสไลด์หน้า 35 แล้วว่า
Region filling ของ Python ให้ตามไปอ่านลิงก์แทน เพราะ OpenCV ไม่มี operation นี้ให้เลือกใช้

### 4. floodFill ทำให้ภาพขาวทั้งภาพ

**อาการ** `204p_0009.jpg` จาก 52,716 พิกเซล กลายเป็น 73,905 พิกเซล คือขาวเกือบทั้งภาพ

**สาเหตุ** ดอกไม้ในภาพนั้นชนขอบภาพพอดี มุม (0,0) จึงเป็นสีขาวคือตัวดอก ไม่ใช่พื้นหลัง
พอเริ่มท่วมสีจากมุมนั้น มันไปท่วมตัวดอกแทนที่จะท่วมพื้นหลัง แล้วผลกลับหัวทั้งภาพ

**วิธีแก้** เติมขอบดำหนา 1 พิกเซลรอบภาพก่อนท่วม แล้วตัดขอบออกทีหลัง
```python
padded_mask = cv2.copyMakeBorder(mask, 1, 1, 1, 1, cv2.BORDER_CONSTANT, value=0)
```
ทำให้มั่นใจว่ามุมภาพเป็นพื้นหลังแน่นอน

### 5. Region Filling ไม่ทำงานเมื่อวงกลีบขาด

**อาการ** `204p_0020.jpg` อุดรูไม่ได้เลย 13,633 -> 13,666 พิกเซล

**สาเหตุ** วงกลีบดอกในภาพนั้นขาดเป็นตัว C รูตรงกลางจึงทะลุออกนอก ไม่ถือว่าเป็นรูที่ถูกล้อมรอบมิดชิด

**วิธีแก้** ใส่ Closing ปิดวงให้ครบก่อน แล้วค่อยอุด
หลังแก้ ภาพเดียวกันได้ 19.6% -> 27.1%

### 6. ติดตั้ง opencv ซ้อนกัน 2 ตัว

**อาการ** `pip list` รายงานว่ามี `opencv-python 4.10.0.84` และ `opencv-contrib-python 5.0.0.93`
แต่ `cv2.__version__` ที่ import ได้จริงคือ `5.0.0`

**สาเหตุ** opencv บน PyPI มีหลายแพ็กเกจที่ลงทับกันที่โฟลเดอร์เดียวกัน (`site-packages/cv2/`)
ตัวที่ลงทีหลังทับตัวที่ลงก่อน แต่ pip ยังรายงานว่ามีทั้งคู่
คือ pip กับความจริงไม่ตรงกัน แล้วจะดีบักไม่ถูกเวลาเจอปัญหา

**วิธีแก้** ใช้ environment แยกที่มี opencv ตัวเดียว
และล็อกเวอร์ชันใน `requirements.txt`
ถ้าเครื่องไหนลงซ้อนกันแล้ว ต้องถอนออกให้หมดทุกตัวก่อนลงใหม่
ห้ามถอนแค่ตัวเดียว เพราะใช้โฟลเดอร์เดียวกัน อาจลบไฟล์ที่อีกตัวต้องใช้ไปด้วย

**เพิ่มเติม** เตือนไว้ในไฟล์ด้วยว่า **ห้ามลง `opencv-python-headless`**
เพราะไม่มี `imshow` ซึ่งทำให้ `step2` ที่ต้องเปิดหน้าต่างระบาย ใช้ไม่ได้

### 7. หน้าต่างระบายสูงเกินจอโน้ตบุ๊ก

**อาการ** ตั้งความสูงหน้าต่างไว้ 700 px

**สาเหตุ** ภาพที่เตี้ยที่สุดคือ `204p_0045.jpg` สูงแค่ 177 px ถูกขยายมากที่สุด
หน้าต่างกลายเป็น 1123×700 บวกแถบหัวหน้าต่างกับ task bar แล้วสูงเกินจอ 1366×768

**วิธีแก้** ลดเหลือ 600 หน้าต่างใหญ่สุดเหลือ 962×600 ใส่ได้ทุกจอที่ใช้กันจริง

### 8. ไฟล์ที่โหลดไม่จบ ค้างอยู่ตลอดไปโดยไม่มีอะไรฟ้อง

**อาการ** เงื่อนไขข้ามไฟล์เดิมเช็คแค่ "มีไฟล์อยู่ไหม" ถ้าเน็ตหลุดตอนกำลังโหลด
จะเหลือไฟล์ที่โหลดไม่จบค้างในชื่อจริง พอรันซ้ำมันข้ามไป ไฟล์เสียค้างตลอดไป

**หลักฐาน** ทดลองตัด `204p_0007.jpg` ให้เหลือครึ่งเดียว
```
cv2.imread ยังอ่านได้ ไม่คืน None
ข้อมูลผิดไป 115 จาก 194 แถว = 59% ของภาพ
step3 ตัดสินว่าเป็นดอกไม้ 25.6% แทนที่จะเป็น 48.4%
ไม่มีจุดไหนใน pipeline ที่ error เลย
```
ที่ร้ายคือจะไปนั่งระบาย ground truth ทับภาพเสียนั้นโดยไม่รู้ตัว

**วิธีที่คิดไว้ตอนแรกแล้วใช้ไม่ได้** ตั้งใจเช็คด้วย `cv2.imread(path) is None`
แต่ทดสอบแล้วมันคืนภาพมาปกติสำหรับไฟล์ที่โหลดไม่จบ จับไม่ได้

**วิธีแก้ที่ใช้จริง** แก้ที่ต้นเหตุแทนการตรวจจับ
```python
temp_path = save_path + ".part"
urllib.request.urlretrieve(image_url, temp_path)
os.replace(temp_path, save_path)
```
บรรทัดสุดท้ายทำงานก็ต่อเมื่อโหลดจบแล้วเท่านั้น ถ้าเน็ตหลุด ชื่อจริงไม่มีวันเกิด
ทดสอบด้วยการจำลองเน็ตหลุดกลางไฟล์ รันซ้ำแล้วได้ไฟล์ครบ 12,111 bytes เท่าต้นฉบับ

### 9. ระบายทั้งที่ไม่ได้กดเมาส์

**อาการ** เดิมจำสถานะปุ่มไว้ในตัวแปร แล้วล้างค่าตอนได้รับ event ปล่อยปุ่ม
แต่ถ้าลากเมาส์ออกนอกหน้าต่างแล้วปล่อยปุ่มตรงนั้น event จะไม่ถูกส่งมา ตัวแปรค้างเป็น True
พอเลื่อนเมาส์กลับเข้ามา มันระบายต่อทั้งที่ไม่ได้กดอะไร

**หลักฐาน** จำลอง event ตรงๆ
```
กดซ้ายค้าง                is_painting = True
เลื่อนเมาส์เฉยๆ (flags=0)  is_painting = True   <- ควรเป็น False
ระบายเพิ่มไป 81 พิกเซลโดยไม่ได้กดปุ่ม
```

**ทำไมร้ายแรง** สิ่งที่เปื้อนคือ ground truth ซึ่งเป็นเฉลยที่ทุกอย่างเอาไปวัด
และเกิดจากการเลื่อนเมาส์เฉยๆ ซึ่งทำตลอดเวลาระหว่างระบาย

**วิธีแก้** เลิกจำสถานะเอง ไปอ่านจาก `flags` ที่ OpenCV ส่งมาให้ทุกครั้งแทน
```python
elif event == cv2.EVENT_MOUSEMOVE:
    if flags & cv2.EVENT_FLAG_LBUTTON:
        draw_brush(x, y, PAINT_COLOR)
```
ใช้ `&` ไม่ใช่ `==` เพราะ `flags` รวมสถานะหลายอย่างไว้ก้อนเดียว ถ้ากด Ctrl ค้างด้วยจะไม่ตรง
ผลพลอยได้ ตัดตัวแปรส่วนกลางออก 2 ตัว และโค้ดจาก 16 เหลือ 8 บรรทัด

### 10. สีเขียวบนจอกว้างกว่าที่ระบายจริง

**อาการ** `cv2.resize` ค่าเริ่มต้นเกลี่ยสีให้ ทำให้ขอบ mask มีค่ากลางๆ โผล่มา
เช่น 4, 12, 20, 28 ทั้งที่ mask จริงมีแค่ 0 กับ 255
แล้วเงื่อนไข `big_mask > 0` กินค่ากลางๆ พวกนั้นเข้ามาด้วย

**หลักฐาน** วัดได้ว่าเขียวบนจอกว้างเกินจริง 10.2%
ทำให้คนระบายเห็นเขียวล้ำออกไป เลยหยุดมือเร็วเกิน แล้วได้ mask เล็กกว่าดอกจริงทุกภาพ

**วิธีแก้** `interpolation=cv2.INTER_NEAREST` วัดใหม่ได้ 43,200 = 43,200 ตรงกันทุกพิกเซล

### 11. import ไฟล์แล้วไปแก้ช่องทาง output ของคนที่ import

**อาการ** วาง `sys.stdout.reconfigure()` ไว้ระดับไฟล์ มันทำงานตอนที่ไฟล์อื่นสั่ง import
และถ้า output ของคนที่ import ไม่มีเมธอด `reconfigure` จะพังตั้งแต่บรรทัด import

**หลักฐาน** `AttributeError: object has no attribute 'reconfigure'`

**ทำไมสำคัญ** `step6` ถูกออกแบบให้ import `step3` กับ `step4` ไปใช้ต่อ

**วิธีแก้** ย้ายเข้าไปเป็นบรรทัดแรกของ `main()` ทั้ง 4 ไฟล์
ตรวจแล้วว่า `print()` ทุกตัวถูกเรียกจาก `main()` เท่านั้น จึงย้ายได้โดยไม่เสียอะไร

### 12. ไฟล์ที่ไม่ใช่ภาพ ทำให้โปรแกรมพัง

**อาการ** ถ้ามีไฟล์อื่นปนในโฟลเดอร์
```
step3 -> cv2.error
step2 -> AttributeError: 'NoneType' object has no attribute 'shape'
```

**สาเหตุ** ไฟล์อื่นเข้ามาได้ 2 ทาง
1. ไฟล์ `.part` ที่ค้างไว้ตอนเน็ตหลุด
2. ไฟล์ `.DS_Store` ที่ macOS สร้างเองทุกครั้งที่เปิดโฟลเดอร์ดูใน Finder

**วิธีแก้** กรองเอาเฉพาะ `.jpg` / `.png` ทุกจุดที่อ่านโฟลเดอร์

### 13. โปรแกรมบอกว่าระบายครบแล้ว ทั้งที่ยังขาด

**อาการ** เป็นบั๊กต่อเนื่องจากข้อ 12 ที่แก้ไม่ครบ
มี `os.listdir` ทั้งหมด 7 จุด แก้ไปแค่ 4 จุดที่วนอ่านไฟล์ ส่วนอีก 3 จุดที่ใช้นับตอนจบยังไม่ได้กรอง

**หลักฐาน** จำลองสภาพเครื่อง Mac
```
ระบายไปแล้วจริง 49 ภาพ + มี .DS_Store 1 ไฟล์
โปรแกรมพิมพ์ "ตอนนี้มี ground truth แล้ว 50 ภาพ จากทั้งหมด 50 ภาพ"
และหายบรรทัด "ยังเหลืออีก N ภาพ" ไปเลย
```
คนระบายจะหยุดเพราะเชื่อว่าเสร็จ ทั้งที่ยังขาด แล้วโจทย์ข้อ 2 ที่บังคับ 50 ภาพก็ไม่ผ่าน

**วิธีแก้** กรองที่การนับด้วย และคราวนี้ใช้ `grep` หาทุกจุดที่เรียก `os.listdir` ก่อนแก้
ไม่ใช่แก้เฉพาะจุดที่ถูกชี้

**บทเรียน** ตอนแก้บั๊กครั้งแรก แก้เฉพาะจุดที่ถูกชี้ ไม่ได้ไล่หาว่ารูปแบบเดียวกันมีที่อื่นอีกไหม

### 14. เครื่องมือวัดผลของตัวเองผิด (2 ครั้ง)

ไม่ใช่บั๊กในโค้ดโปรเจกต์ แต่บันทึกไว้เพราะเกือบทำให้สรุปผิด

**ครั้งที่ 1** ตอนทดสอบว่าสีเขียวบนจอตรงกับที่ระบายไหม นับ "พิกเซลเขียว" จากสูตร G−B > 40
ได้ 211,379 px ซึ่งเกือบทั้งจอ เกือบสรุปว่าแก้ไม่สำเร็จ
**สาเหตุ** ดอกทานตะวันสีเหลืองก็มี G สูง B ต่ำ เลยถูกนับไปด้วย
**แก้** เปลี่ยนไปวัดว่า "พิกเซลไหนบนจอถูกเปลี่ยนไปจากภาพต้นฉบับ" แทนการเดาจากสี

**ครั้งที่ 2** วัดแล้วเกินไป 210 px ตามไปดูพบว่าเป็นส่วนล่างของวงกลมแดงบอกขนาดแปรง
ที่วาดไว้ที่จุด (40,70) รัศมี 36 กินถึงแถว 106 แต่ตัดโซนออกแค่แถว 0–90
**แก้** ย้ายจุดทดสอบไปโซนที่ไม่มีของตกแต่ง ได้ 43,200 = 43,200 ตรงกันทุกพิกเซล

### 15. conda เรียกไม่เจอ

**อาการ** พิมพ์ `conda` แล้วขึ้นว่าไม่รู้จักคำสั่ง ทั้งที่ติดตั้งไว้แล้ว

**สาเหตุ** ตอนติดตั้งไม่ได้เลือกให้ลงทะเบียนใน PATH

**วิธีแก้** เปิดโปรแกรม Anaconda Prompt แทน terminal ธรรมดา
หรือลงทะเบียนถาวรครั้งเดียวด้วย `conda init powershell` แล้วปิดเปิดหน้าต่างใหม่

---

## การตัด def ออก

จาก 18 เหลือ 15 เกณฑ์ที่ใช้: **ตัดถ้าชื่อ `def` ไม่ได้บอกอะไรเพิ่มจากบรรทัดที่มันห่อไว้**

| ตัดออก | เหตุผล |
|---|---|
| `save_mask()` ใน `step2` และ `step3` | ห่อ `cv2.imwrite()` เฉยๆ เรียกที่เดียว ชื่อไม่ได้เพิ่มข้อมูล |
| `make_structuring_element()` ใน `step4` | ห่อ `cv2.getStructuringElement()` บรรทัดเดียว มีอยู่เพื่อเก็บคอมเมนต์ ย้ายคอมเมนต์ไปไว้ที่ค่าคงที่แทน |

**ไม่ตัด** `remove_small_spots()` และ `close_small_gaps()` ทั้งที่ตัวละ 2 บรรทัดและเรียกที่เดียว
เพราะชื่อบอกว่ามัน **ทำอะไรให้เรา** ซึ่ง `cv2.morphologyEx(mask, cv2.MORPH_OPEN, se)` ไม่ได้บอก
และเป็นชื่อที่ตรงกับที่สไลด์หน้า 24 เรียก ครูชี้มาแล้วตอบได้ทันที

---

## วิธีตรวจว่าไม่มีอะไรพัง

ใช้ **ลายนิ้วมือของผลลัพธ์** — เอาไฟล์ mask ทั้ง 50 ไฟล์มาคำนวณ md5 รวมกัน
ถ้าแก้โค้ดแล้วค่านี้ไม่เปลี่ยน แปลว่าผลลัพธ์เหมือนเดิมทุกบิต

```
ก่อนแก้ทั้งหมด          e6f5a92a489d622417556fae8411c25a
หลังตัด def 3 ตัว        e6f5a92a489d622417556fae8411c25a
หลังแก้บั๊กทั้งหมด        e6f5a92a489d622417556fae8411c25a
รันบน Python 3.9        e6f5a92a489d622417556fae8411c25a
รันบน Python 3.12       e6f5a92a489d622417556fae8411c25a
```

ยังทดสอบเพิ่มว่า opencv รุ่น `4.13.0.92` กับ `5.0.0.93` ให้ผลเหมือนกันทุกบิต
จึงล็อกรุ่น 4.13 ใน `requirements.txt` เพราะติดตั้งได้ทั้ง Python 3.9 และ 3.12

---

## ตรวจความเป็นไปได้ของ ROC ล่วงหน้า

ก่อนเขียน `step6` ตรวจ 2 อย่างที่ถ้าไม่ผ่านจะเขียนไม่ได้

**1. กวาดค่าตัดแล้วไปถึงปลายทั้งสองข้างของเส้นไหม**
ค่า Lab ช่อง b ที่ต่ำที่สุดของทั้ง dataset = 49 ซึ่งมากกว่า 0
แปลว่ากวาดตั้งแต่ 0 ถึง 255 ได้ครบเส้น ปลายล่าง 100% ปลายบน 0%

**2. เส้นจะไต่ขึ้นเรื่อยๆ หรือหยักไปมา**
ทดสอบกับภาพที่ยากที่สุด (`204p_0009`) 51 ระดับค่าตัด
พบว่าเซ็ตพิกเซลซ้อนกันดีตลอด ไม่มีการหดกลับเลยสักครั้ง ทั้งก่อนและหลัง morphology
แปลว่าเส้น ROC จะเรียบ อธิบายได้

---

## ข้อจำกัดที่รู้อยู่แล้ว ยังไม่แก้

- **`204p_0012.jpg`** เป็นภาพโคลสอัพที่ดอกกินเกือบเต็มเฟรม พื้นหลังเหลือแค่แถบริมขอบ
  หลัง morphology ใบไม้ริมขอบถูกกลืนไปด้วย 79.3% -> 97.8%
  ยังไม่ปรับขนาด SE เพราะยังไม่มี ground truth จึงยังวัดไม่ได้ว่าปรับแล้วดีขึ้นจริงไหม
  รอให้ `step5` กับ `step6` ตอบด้วยตัวเลข

- ในกลุ่มรูปถ่ายมีภาพที่เป็นภาพวาดปนมาบ้าง (0004, 0022, 0038)
  และภาพที่มีตัวหนังสือทับ (0030, 0041) ไม่ได้ตัดออก
  เก็บไว้ดูว่าผลแย่ลงแค่ไหน แล้วเอาไปเขียนในส่วนอภิปรายผล

- เครื่องมือระบายยังไม่มีวิธีตรวจ ground truth หลังระบายเสร็จ
  ถ้าเผลอกด `s` ตั้งแต่ยังระบายไม่เสร็จ รอบหน้าโปรแกรมจะข้ามภาพนั้นให้อัตโนมัติ
  ยังไม่เขียนเพราะยังไม่รู้ว่าเป็นปัญหาจริงไหม รอระบายจริงไปสักพักก่อน

---

## งานที่เหลือ

| โจทย์ | สถานะ |
|---|---|
| ข้อ 1 หาโจทย์ segmentation | เสร็จ |
| ข้อ 2 dataset 50 ภาพ | เสร็จ |
| ข้อ 2 ground truth | **ยัง 0 จาก 50 ภาพ** |
| ข้อ 3 algorithm แยกวัตถุ | เสร็จ |
| ข้อ 4 morphology | เสร็จ |
| ข้อ 5 Confusion Matrix | รอ ground truth |
| ข้อ 5 ROC Curve | รอ ground truth |
| ข้อ 6 นำเสนอ | รอผล |

**แบ่งงานระบาย ground truth คนละ 25 ภาพ** โดยแก้ค่า `START_FROM_IMAGE` ในไฟล์ `step2`
คนแรกตั้ง 1 ระบายภาพ 0001-0025 อีกคนตั้ง 26 ระบายภาพ 0026-0050
รายละเอียดอยู่ใน issue ของโปรเจกต์
